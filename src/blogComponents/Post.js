import React from 'react';

export default class Post extends React.Component {
    //In this component we receive blog posts which are then displayed in blog.js
    //makes request to database
  render() {
      function findPost(){
          //... finds post... and loads it
          return
      }
    return (
        <div>
            TEST
        </div>
);
  }
}
